[project]
name = "drunk-poetry-downloader"
version = "0.1.0"
description = "Download all videos from a YouTube channel (DrunkPoetry) with yt-dlp"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "yt-dlp>=2026.8.19",
    "requests>=2.32",
]
